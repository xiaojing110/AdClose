name: Build Android

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - '**.txt'
      - '.idea/**'
      - '!.github/workflows/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'adopt'
          java-version: '21'
          cache: gradle

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK components
        run: |
          sdkmanager --install \
            "platforms;android-36" \
            "build-tools;36.0.0" \
            "ndk;28.2.13676358" \
            "cmake;3.22.1"

      - name: Grant execute permission
        run: chmod +x ./gradlew

      - name: Build debug APK
        run: ./gradlew :app:assembleDebug --no-daemon

      - name: Package release zip
        run: |
          mkdir -p release
<<<<<<< HEAD
          cp app/build/outputs/apk/debug/*.apk release/ 2>/dev/null || true
          cd release && zip -r ../AdClose.zip . && cd ..
=======
          cp -r app/build/outputs/apk/release/*.apk release/ 2>/dev/null || true
          cp -r app/build/outputs/mapping/ release/mapping/ 2>/dev/null || true
          cd release && zip -r ../AdClose-${{ github.sha }}.zip . && cd ..
>>>>>>> parent of ddb561f (增加云更新)

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: AdClose-apk
          path: app/build/outputs/apk/debug/*.apk

      - name: Upload ZIP
        uses: actions/upload-artifact@v4
        with:
<<<<<<< HEAD
          name: AdClose-zip
          path: AdClose.zip
=======
          name: AdClose-${{ github.sha }}
          path: AdClose-${{ github.sha }}.zip
          compression-level: 9
>>>>>>> parent of ddb561f (增加云更新)

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
<<<<<<< HEAD
            AdClose.zip
            app/build/outputs/apk/debug/*.apk
          generate_release_notes: true
=======
            AdClose-${{ github.sha }}.zip
            app/build/outputs/apk/release/*.apk
>>>>>>> parent of ddb561f (增加云更新)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
